# Copy to .env.local and fill in to run LIVE (shared team data via Supabase).
# Without these, the app falls back to local browser storage (preview only).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_PASSCODE=1234
